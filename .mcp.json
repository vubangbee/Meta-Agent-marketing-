{
  "mcpServers": {
    "meta-agent-browser": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--user-data-dir", "${CLAUDE_PROJECT_DIR:-.}/browser-profile", "--browser", "chrome"]
    }
  }
}
